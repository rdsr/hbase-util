#!/usr/bin/env bash

mkdir -p logs
export HBASE_CLASSPATH=`hbase classpath`
echo "Starting shell"
${JAVA_HOME}/bin/java -Djava.security.auth.login.config=conf/jaas-hbase-client.conf -Dfile.encoding=UTF-8 -cp "conf:lib/*:$HBASE_CLASSPATH" reply.ReplyMain --eval "(use 'hbase-util.core)"
